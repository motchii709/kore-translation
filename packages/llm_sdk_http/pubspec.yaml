name: llm_sdk_http
description: Platform-agnostic streaming HTTP transport for the kore LLM SDK – dio on native, Fetch API on the web.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  dio: ^5.11.0
  web: ^1.1.1

dev_dependencies:
  kore_lints: ^0.1.0
  test: ^1.26.2
