targets:
  $default:
    builders:
      json_serializable:
        options:
          # Schema mismatches surface as CheckedFromJsonException instead of
          # TypeError.
          checked: true
